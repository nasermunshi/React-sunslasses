const add = (first,secound) =>{
return first + secound;
}

const multiplay = (first, secound)=>{
  return first * secound;                  
}

const devidednumber = (first, secound)=>{
 return first /secound;                   
}

export {add, multiplay, devidednumber}
// export default add;
// const calculator = {add: add}
// export default calculator;
